[phases.setup]
nixPkgs = ["ffmpeg", "curl"]

[phases.install]
cmds = [
  "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp_linux -o yt-dlp",
  "chmod +x yt-dlp",
  "npm ci"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
